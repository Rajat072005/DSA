class Solution {
public:
    bool checkOnesSegment(string s) {
        int ans = 1;
        for(int i = 1 ; i<s.size() ; i++){
            if(s[i] == '1' && s[i-1] != '1')ans++;
        }
        return (ans == 1 ? true : false);
    }
};
